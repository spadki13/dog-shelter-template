# Globals

- `SiteSettings` — built. Branding fields (logo, colors, name, contact info) + feature toggle checkboxes (enable donations / enable merch / enable adoption applications). Frontend reads this at render time to show/hide sections.
